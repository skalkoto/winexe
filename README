winexe
======

Build instructions
------------------

Instruction is for Ubuntu 12.10,
but similar steps can be performed on any other distribution.

Install common dependencies:

    sudo apt-get install gcc-mingw-w64

Install dependencies for shared version:

    sudo apt-get install libdcerpc-dev libpopt-dev libsamba-credentials-dev
    sudo apt-get install libsamba-util-dev libsmbclient-raw-dev libtevent-dev
    sudo apt-get install samba4-dev libtalloc-dev

Install dependencies for static version:

    sudo apt-get install comerr-dev libpopt-dev libbsd-dev zlib1g-dev libc6-dev

Download samba sources for static version:

    git clone git://git.samba.org/samba.git samba

Build shared version:

    cd source
    ./waf configure
    ./waf

For static version you should pass location of samba dir:

    ./waf configure --samba-dir=...

Build system try to find paths to includes/libs,
but you can provide it directly using configuration options:
    --samba-inc-dirs
    --samba-lib-dirs

If you are interested only in static version of winexe
you can use configuration option:
    --disable-shared

Credits
-------
Written by Andrzej Hajda <andrzej.hajda@wp.pl>

Contributions by:
* Thomas Hood, RAAF Technology <thomas@raaftech.nl>

License
-------
Please see the COPYING file for the text of the GNU GPL version 3.
